### Backwards-incompatible changes

- Rename configuration settings `url_prefix` to `v1_path_prefix` and `v2_url_prefix` to `path_prefix`, and rename `profile` to `log_profile`. This naming more closely matches other projects.
